/**
 * app.config is used to expose public variables that can be determined at build time,
 * such as theme variants, titles, or other non-sensitive project configurations.
 * These values are set in the app.config.ts file.
 */

export default defineAppConfig({
  theme: {
    primaryColor: '#ababab',
  },
})
